Both directories contain may the same files but are verydifferent. For the two directories:
1."Used" is the directory with the scripts that were used in the lab work
2."Unused" is the directory with scripts that must be edited to achieve the same results as the 'Used' directory on another computer